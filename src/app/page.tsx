/**
 * Root page — tenant landing / event listing (placeholder for Stage 1).
 *
 * @returns A placeholder indicating the scaffold is running correctly.
 */
export default function HomePage() {
  return (
    <main>
      <h1>TenantKit</h1>
      <p>Scaffold ready. Stage 1 will replace this page.</p>
    </main>
  );
}
